class Food{
    constructor(foodStock, lastFed, ) {
        var options = {
            'restitution':0.8,
            'friction':1.0,
            'density':1.0
        }

      }
      display(){
       // if else 

       var bedroom, garden, washroom;


        
        push();
        translate(this.body.position.x, this.body.position.y);
        rotate(angle);
        imageMode(CENTER);
        image(this.image, 0, 0, this.width, this.height);
        pop();
      }
}//this.image = loadImage("js/.png");